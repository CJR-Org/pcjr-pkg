PCJR (CJR Package Manager) distributed as a moduland package, requires sudo privileges.  

# Install
`pcjr install pcjr`  
  
A packages.json file and modules folder will remain after the install and can be removed if not being used.